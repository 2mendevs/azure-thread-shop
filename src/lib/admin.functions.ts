// Intentionally empty — legacy server-fn module replaced by local-storage helpers
// in src/lib/use-products.ts and src/lib/orders.ts. Kept as an empty stub so any
// stale imports fail loudly during typecheck rather than silently misbehave.
export {};
